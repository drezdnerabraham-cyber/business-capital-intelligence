# Copy this file to .env and fill in values

# Port for the server (default: 3000)
PORT=3000

# Optional: set a secret so only your site can trigger the webhook
# Add header X-Webhook-Secret: <your-secret> from your form tool
WEBHOOK_SECRET=change-me-to-something-random
